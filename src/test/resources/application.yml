spring:
  datasource:
    url: jdbc:tc:postgresql:///westbranchmanagement
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    schemas: accounts
    locations: classpath:migration